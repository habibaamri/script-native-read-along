import common = require("./slider-common");
import style = require("ui/styling/style");
export declare class Slider extends common.Slider {
    private _ios;
    private _changeHandler;
    constructor();
    readonly ios: UISlider;
}
export declare class SliderStyler implements style.Styler {
    private static setColorProperty(view, newValue);
    private static resetColorProperty(view, nativeValue);
    private static getNativeColorValue(view);
    private static setBackgroundColorProperty(view, newValue);
    private static resetBackgroundColorProperty(view, nativeValue);
    private static getBackgroundColorProperty(view);
    static registerHandlers(): void;
}
